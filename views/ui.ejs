<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Sistema de Inventario</title>

    <!-- Google Font – Poppins -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap"
        rel="stylesheet">

    <style>
        :root {
            --wa-green: #25D366;
            --wa-dark: #128C7E;
            --bg: #f0f7f4;
            --card: #ffffff;
            --text: #2d3748;
            --shadow: 0 10px 30px rgba(0, 0, 0, 0.07);
            --border: #e2e8f0;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #e6f5ee, #c2e8d6);
            color: var(--text);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .card {
            background: var(--card);
            max-width: 520px;
            width: 100%;
            border-radius: 24px;
            overflow: hidden;
            box-shadow: var(--shadow);
            padding: 40px 30px;
            text-align: center;
        }

        /* Simple circular accent without icon */
        .logo {
            width: 84px;
            height: 84px;
            background: var(--wa-green);
            border-radius: 50%;
            margin: 0 auto 20px;
            box-shadow: 0 6px 20px rgba(37, 211, 102, .35);
        }

        h1 {
            font-size: 28px;
            font-weight: 600;
            margin-bottom: 12px;
            color: var(--wa-dark);
        }

        .subtitle {
            font-size: 16px;
            color: #555;
            margin-bottom: 30px;
        }

        .form-group {
            margin-bottom: 20px;
            text-align: left;
        }

        label {
            display: block;
            font-weight: 500;
            margin-bottom: 8px;
            color: var(--wa-dark);
        }

        textarea {
            width: 100%;
            min-height: 130px;
            padding: 14px;
            border: 2px solid var(--border);
            border-radius: 12px;
            font-family: inherit;
            font-size: 15px;
            transition: border 0.2s, box-shadow 0.2s;
        }

        textarea:focus {
            outline: none;
            border-color: var(--wa-green);
            box-shadow: 0 0 0 3px rgba(37, 211, 102, .15);
        }

        .required::after {
            content: " *";
            color: #e53e3e;
        }

        .invalid textarea {
            border-color: #e53e3e;
        }

        .invalid .error {
            color: #e53e3e;
            font-size: 13px;
            margin-top: 4px;
            display: block;
        }

        .btn {
            background: var(--wa-green);
            color: #fff;
            border: none;
            padding: 12px 32px;
            font-size: 16px;
            font-weight: 600;
            border-radius: 12px;
            cursor: pointer;
            transition: background .25s, transform .1s;
            display: inline-flex;
            align-items: center;
            gap: 8px;
        }

        .btn:hover {
            background: var(--wa-dark);
        }

        .btn:active {
            transform: translateY(1px);
        }

        .footer {
            margin-top: 30px;
            font-size: 13px;
            color: #777;
        }
    </style>
</head>

<body>
    <div class="card">
        <!-- Clean green circle – no icon -->
        <div class="logo"></div>

        <h1>Sistema de Inventario</h1>
        <p class="subtitle">Ingresa los datos del conteo y presiona <strong>Terminar conteo</strong> para procesar.</p>

        <form action="/conteo_input" method="POST" class="validated-form">

            <div class="form-group">
                <label for="description" class="required">Iniciar Conteo</label>
                <textarea class="form-control" id="description" name="IDs"
                    placeholder="Escribe aquí los EPCs, notas o sube un CSV..." required></textarea>
                <span class="error" style="display:none;">Este campo es obligatorio.</span>
            </div>

            <div class="form-group">
                <button type="submit" class="btn">
                    Terminar conteo
                </button>
            </div>
        </form>

        <div class="footer">
            Powered by ON Management &amp; AWS
        </div>
    </div>

    <!-- Client-side validation -->
    <script>
        const form = document.querySelector('.validated-form');
        const textarea = document.getElementById('description');
        const errorSpan = textarea.parentElement.querySelector('.error');

        form.addEventListener('submit', e => {
            if (!textarea.value.trim()) {
                e.preventDefault();
                textarea.parentElement.classList.add('invalid');
                errorSpan.style.display = 'block';
            } else {
                textarea.parentElement.classList.remove('invalid');
                errorSpan.style.display = 'none';
            }
        });
    </script>
</body>

</html>