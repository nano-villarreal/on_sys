<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Desglose de Daños - <%= client %>
    </title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap"
        rel="stylesheet">
    <style>
        :root {
            --wa-green: #25D366;
            --wa-dark: #128C7E;
            --card: #ffffff;
            --shadow: 0 10px 30px rgba(0, 0, 0, 0.07);
            --border: #e2e8f0;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #e6f5ee, #c2e8d6);
            color: #2d3748;
            min-height: 100vh;
            padding-top: 90px;
        }

        /* NAVBAR - Same as dashboard */
        .navbar {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            height: 70px;
            background: var(--card);
            box-shadow: var(--shadow);
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 0 30px;
            z-index: 1000;
        }

        .navbar .logo-title {
            display: flex;
            align-items: center;
            gap: 14px;
            font-weight: 600;
            font-size: 20px;
            color: var(--wa-dark);
        }

        .navbar .logo-title .logo-small {
            width: 40px;
            height: 40px;
            background: var(--wa-green);
            border-radius: 50%;
            box-shadow: 0 4px 12px rgba(37, 211, 102, .4);
        }

        .navbar .nav-links {
            display: flex;
            gap: 20px;
        }

        .navbar .nav-links a {
            padding: 10px 20px;
            background: var(--wa-green);
            color: white;
            font-weight: 600;
            text-decoration: none;
            border-radius: 12px;
            transition: all 0.2s;
        }

        .navbar .nav-links a:hover {
            background: var(--wa-dark);
            transform: translateY(-2px);
        }

        .navbar .nav-links a.active {
            background: var(--wa-dark);
        }

        @media (max-width: 768px) {
            .navbar {
                flex-direction: column;
                height: auto;
                padding: 15px;
            }

            .navbar .nav-links {
                margin-top: 15px;
                gap: 12px;
                flex-wrap: wrap;
                justify-content: center;
            }
        }

        /* CONTENT */
        .container {
            max-width: 1100px;
            margin: 30px auto;
            background: var(--card);
            padding: 40px;
            border-radius: 24px;
            box-shadow: var(--shadow);
        }

        h1 {
            color: var(--wa-dark);
            text-align: center;
            font-size: 32px;
            margin-bottom: 10px;
        }

        .subtitle {
            text-align: center;
            color: #666;
            margin-bottom: 30px;
        }

        .summary-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
            gap: 20px;
            margin: 30px 0;
        }

        .summary-card {
            background: #ffebee;
            padding: 24px;
            border-radius: 16px;
            text-align: center;
            border: 2px solid #ffcdd2;
        }

        .summary-card h3 {
            color: #c62828;
            margin-bottom: 12px;
        }

        .summary-card p {
            font-size: 36px;
            font-weight: bold;
            color: #b71c1c;
        }

        .breakdown {
            margin: 40px 0;
        }

        .breakdown h2 {
            text-align: center;
            margin-bottom: 20px;
            color: var(--wa-dark);
        }

        .breakdown-list {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 16px;
        }

        .breakdown-item {
            background: #fff3e0;
            padding: 16px;
            border-radius: 12px;
            border-left: 5px solid #ff8f00;
        }

        .breakdown-item strong {
            color: #e65100;
        }

        .damage-list {
            margin-top: 50px;
        }

        .damage-list h2 {
            text-align: center;
            margin-bottom: 30px;
            color: var(--wa-dark);
        }

        .damage-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 24px;
        }

        .damage-item {
            background: white;
            border-radius: 16px;
            overflow: hidden;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.08);
            text-align: center;
        }

        .damage-item img {
            width: 100%;
            height: 220px;
            object-fit: cover;
        }

        .damage-info {
            padding: 16px;
        }

        .damage-info .epc {
            font-weight: bold;
            color: var(--wa-dark);
            font-size: 18px;
        }

        .damage-info .date {
            color: #666;
            font-size: 14px;
            margin: 8px 0;
        }

        .damage-info .desc {
            color: #444;
            margin-top: 10px;
            font-size: 15px;
        }

        .error {
            text-align: center;
            color: #d32f2f;
            background: #ffebee;
            padding: 20px;
            border-radius: 12px;
            margin: 20px 0;
        }

        a.back {
            display: block;
            text-align: center;
            margin-top: 50px;
            color: var(--wa-dark);
            font-weight: 600;
            text-decoration: none;
            font-size: 17px;
        }
    </style>
</head>

<body>
    <!-- NAVBAR -->
    <nav class="navbar">
        <div class="logo-title">
            <div class="logo-small"></div>
            <span>Sistema de Inventario</span>
        </div>
        <div class="nav-links">
            <a href="/?client=<%= encodeURIComponent(client || '') %>">Volver al Conteo</a>
            <a href="/create_defect?client=<%= encodeURIComponent(client || '') %>">Reporte de Daño</a>
            <a href="/damage-breakdown?client=<%= encodeURIComponent(client || '') %>" class="active">Desglose de
                Daños</a>
            <a href="/log?client=<%= encodeURIComponent(client || '') %>">Bitácora de Recolección y Entrega</a>
            <a href="/dashboard?client=<%= encodeURIComponent(client || '') %>">Dashboard</a>
        </div>
    </nav>

    <div class="container">
        <h1>Desglose de Daños</h1>
        <p class="subtitle">Cliente: <strong>
                <%= client %>
            </strong></p>

        <% if (error) { %>
            <div class="error">
                <%= error %>
            </div>
            <% } else if (totalDamaged===0) { %>
                <p style="text-align: center; font-size: 20px; color: #4caf50; margin: 40px 0;">
                    ¡Excelente! No hay prendas dañadas reportadas para este cliente.
                </p>
                <% } else { %>

                    <!-- Total Summary -->
                    <div class="summary-grid">
                        <div class="summary-card">
                            <h3>Total de Prendas Dañadas</h3>
                            <p>
                                <%= totalDamaged %>
                            </p>
                        </div>
                    </div>

                    <!-- Breakdown by Article -->
                    <% if (articleBreakdown.length> 0) { %>
                        <div class="breakdown">
                            <h2>Daños por Artículo</h2>
                            <div class="breakdown-list">
                                <% articleBreakdown.forEach(([article, count])=> { %>
                                    <div class="breakdown-item">
                                        <strong>
                                            <%= article %>
                                        </strong>: <%= count %> dañada<%= count> 1 ? 's' : '' %>
                                    </div>
                                    <% }) %>
                            </div>
                        </div>
                        <% } %>

                            <!-- Individual Damage Reports -->
                            <div class="damage-list">
                                <h2>Reportes Detallados</h2>
                                <div class="damage-grid">
                                    <% damageReports.forEach(report=> { %>
                                        <div class="damage-item">
                                            <% if (report.imageUrl) { %>
                                                <img src="<%= report.imageUrl %>" alt="Daño en <%= report.epc %>">
                                                <% } else { %>
                                                    <div
                                                        style="height: 220px; background: #eee; display: flex; align-items: center; justify-content: center; color: #999;">
                                                        Sin imagen
                                                    </div>
                                                    <% } %>
                                                        <div class="damage-info">
                                                            <div class="epc">EPC: <%= report.epc %>
                                                            </div>
                                                            <div class="date">
                                                                <%= new Date(report.date).toLocaleDateString('es-MX', {
                                                                    year: 'numeric' , month: 'long' , day: 'numeric' ,
                                                                    hour: '2-digit' , minute: '2-digit' }) %>
                                                            </div>
                                                            <div class="desc">
                                                                <%= report.description %>
                                                            </div>
                                                        </div>
                                        </div>
                                        <% }) %>
                                </div>
                            </div>

                            <% } %>

                                <a href="/dashboard?client=<%= encodeURIComponent(client || '') %>" class="back">←
                                    Volver al Dashboard</a>
    </div>
</body>

</html>