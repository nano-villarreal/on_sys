<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - Inventario por Cliente</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap"
        rel="stylesheet">
    <style>
        :root {
            --wa-green: #25D366;
            --wa-dark: #128C7E;
            --bg: #f0f7f4;
            --card: #ffffff;
            --text: #2d3748;
            --shadow: 0 10px 30px rgba(0, 0, 0, 0.07);
            --border: #e2e8f0;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #e6f5ee, #c2e8d6);
            color: var(--text);
            min-height: 100vh;
            padding-top: 90px;
            /* Espacio para navbar fija */
        }

        /* ===== NAVBAR ===== */
        .navbar {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            height: 70px;
            background: var(--card);
            box-shadow: var(--shadow);
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 0 30px;
            z-index: 1000;
        }

        .navbar .logo-title {
            display: flex;
            align-items: center;
            gap: 14px;
            font-weight: 600;
            font-size: 20px;
            color: var(--wa-dark);
        }

        .navbar .logo-title .logo-small {
            width: 40px;
            height: 40px;
            background: var(--wa-green);
            border-radius: 50%;
            box-shadow: 0 4px 12px rgba(37, 211, 102, .4);
        }

        .navbar .nav-links {
            display: flex;
            gap: 20px;
        }

        .navbar .nav-links a {
            padding: 10px 20px;
            background: var(--wa-green);
            color: white;
            font-weight: 600;
            text-decoration: none;
            border-radius: 12px;
            transition: all 0.2s;
            box-shadow: 0 4px 12px rgba(37, 211, 102, .3);
        }

        .navbar .nav-links a:hover {
            background: var(--wa-dark);
            transform: translateY(-2px);
        }

        .navbar .nav-links a.active {
            background: var(--wa-dark);
        }

        /* Responsive navbar */
        @media (max-width: 768px) {
            .navbar {
                flex-direction: column;
                height: auto;
                padding: 15px;
            }

            .navbar .nav-links {
                margin-top: 15px;
                flex-wrap: wrap;
                justify-content: center;
                gap: 12px;
            }

            .navbar .nav-links a {
                padding: 10px 18px;
                font-size: 15px;
            }
        }

        /* ===== CONTENIDO ===== */
        .container {
            max-width: 1100px;
            margin: 30px auto;
            background: var(--card);
            padding: 40px;
            border-radius: 24px;
            box-shadow: var(--shadow);
        }

        h1 {
            color: var(--wa-dark);
            text-align: center;
            font-size: 32px;
            margin-bottom: 10px;
        }

        form {
            text-align: center;
            margin-bottom: 40px;
        }

        input[type="text"] {
            padding: 14px 18px;
            font-size: 18px;
            width: 380px;
            border: 2px solid var(--border);
            border-radius: 12px;
            font-family: inherit;
        }

        input[type="text"]:focus {
            outline: none;
            border-color: var(--wa-green);
            box-shadow: 0 0 0 3px rgba(37, 211, 102, .15);
        }

        button {
            padding: 14px 28px;
            font-size: 18px;
            background: var(--wa-green);
            color: white;
            border: none;
            border-radius: 12px;
            cursor: pointer;
            margin-left: 12px;
            font-weight: 600;
        }

        button:hover {
            background: var(--wa-dark);
        }

        .summary {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
            gap: 20px;
            margin: 30px 0;
        }

        .card {
            background: #e8f5e9;
            padding: 24px;
            border-radius: 16px;
            text-align: center;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.05);
        }

        .card h3 {
            margin-bottom: 12px;
            color: var(--wa-dark);
        }

        .card p {
            font-size: 36px;
            font-weight: bold;
            margin: 0;
            color: var(--text);
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin: 40px 0;
        }

        th,
        td {
            padding: 14px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }

        th {
            background: var(--wa-dark);
            color: white;
            font-weight: 600;
        }

        tr:hover {
            background: #f0f7f4;
        }

        .avg {
            font-weight: bold;
            color: var(--wa-green);
        }

        .error {
            color: #d32f2f;
            background: #ffebee;
            padding: 16px;
            border-radius: 12px;
            margin: 20px 0;
            text-align: center;
            font-weight: 500;
        }

        a.back {
            display: block;
            text-align: center;
            margin-top: 50px;
            color: var(--wa-dark);
            font-weight: 600;
            text-decoration: none;
            font-size: 17px;
        }

        a.back:hover {
            text-decoration: underline;
        }
    </style>
</head>

<body>
    <!-- NAVBAR -->
    <nav class="navbar">
        <div class="logo-title">
            <div class="logo-small"></div>
            <span>Sistema de Inventario</span>
        </div>
        <a href="/?client=<%= encodeURIComponent(client || '') %>">Volver al Conteo</a>
        <a href="/create_defect?client=<%= encodeURIComponent(client || '') %>">Reporte de Daño</a>
        <a href="/damage-breakdown?client=<%= encodeURIComponent(client || '') %>">Desglose de Daños</a>
        <a href="/log?client=<%= encodeURIComponent(client || '') %>">Bitácora de Recolección y Entrega</a>
        <a href="/dashboard?client=<%= encodeURIComponent(client || '') %>" class="active">Dashboard</a>
        </div>
    </nav>

    <div class="container">
        <h1>Dashboard de Cliente</h1>

        <form method="POST" action="/dashboard">
            <input type="text" name="client" placeholder="Nombre del cliente (ej: Club Country)"
                value="<%= client || '' %>" required autofocus />
            <button type="submit">Buscar</button>
        </form>

        <% if (error) { %>
            <div class="error">
                <%= error %>
            </div>
            <% } %>

                <% if (stats) { %>
                    <h2 style="text-align: center; margin: 30px 0;">Inventario de: <strong>
                            <%= client %>
                        </strong></h2>

                    <div class="summary">
                        <div class="card">
                            <h3>Total de Prendas</h3>
                            <p>
                                <%= stats.totalItems %>
                            </p>
                        </div>
                        <div class="card">
                            <h3>Lavados Totales</h3>
                            <p>
                                <%= stats.totalWashCount %>
                            </p>
                        </div>
                        <div class="card">
                            <h3>Promedio General</h3>
                            <p>
                                <%= overallAverage %>
                            </p>
                        </div>
                        <div class="card">
                            <h3>Prendas Dañadas</h3>
                            <p>
                                <%= stats.damagedCount %>
                            </p>
                        </div>
                    </div>

                    <% if (articleAverages && articleAverages.length> 0) { %>
                        <h2 style="text-align: center; margin-top: 60px;">Promedio de Lavados por Artículo</h2>
                        <table>
                            <thead>
                                <tr>
                                    <th>Artículo</th>
                                    <th>Cantidad</th>
                                    <th>Lavados Totales</th>
                                    <th class="avg">Promedio de Lavados</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% articleAverages.forEach(item=> { %>
                                    <tr>
                                        <td><strong>
                                                <%= item._id || 'Sin artículo' %>
                                            </strong></td>
                                        <td>
                                            <%= item.itemCount %>
                                        </td>
                                        <td>
                                            <%= item.totalWashes %>
                                        </td>
                                        <td class="avg">
                                            <%= item.avgWashes.toFixed(1) %>
                                        </td>
                                    </tr>
                                    <% }) %>
                            </tbody>
                        </table>
                        <% } else if (client) { %>
                            <p style="text-align: center; font-size: 18px; color: #666;">
                                No hay artículos registrados para este cliente.
                            </p>
                            <% } %>

                                <% } else if (client) { %>
                                    <p style="text-align: center; font-size: 18px; color: #666;">
                                        No se encontraron datos para el cliente: <strong>
                                            <%= client %>
                                        </strong>
                                    </p>
                                    <% } %>

                                        <a href="/" class="back">← Volver al inicio</a>
    </div>
</body>

</html>