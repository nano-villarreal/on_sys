<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bitácora de Recolección y Entrega - <%= client %>
    </title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap"
        rel="stylesheet">
    <style>
        :root {
            --wa-green: #25D366;
            --wa-dark: #128C7E;
            --card: #ffffff;
            --shadow: 0 10px 30px rgba(0, 0, 0, 0.07);
            --border: #e2e8f0;
            --recoleccion: #e3f2fd;
            --entrega: #e8f5e9;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #e6f5ee, #c2e8d6);
            color: #2d3748;
            min-height: 100vh;
            padding-top: 90px;
        }

        /* NAVBAR - Same as other pages */
        .navbar {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            height: 70px;
            background: var(--card);
            box-shadow: var(--shadow);
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 0 30px;
            z-index: 1000;
        }

        .navbar .logo-title {
            display: flex;
            align-items: center;
            gap: 14px;
            font-weight: 600;
            font-size: 20px;
            color: var(--wa-dark);
        }

        .navbar .logo-title .logo-small {
            width: 40px;
            height: 40px;
            background: var(--wa-green);
            border-radius: 50%;
            box-shadow: 0 4px 12px rgba(37, 211, 102, .4);
        }

        .navbar .nav-links {
            display: flex;
            gap: 20px;
        }

        .navbar .nav-links a {
            padding: 10px 20px;
            background: var(--wa-green);
            color: white;
            font-weight: 600;
            text-decoration: none;
            border-radius: 12px;
            transition: all 0.2s;
        }

        .navbar .nav-links a:hover {
            background: var(--wa-dark);
            transform: translateY(-2px);
        }

        .navbar .nav-links a.active {
            background: var(--wa-dark);
        }

        @media (max-width: 768px) {
            .navbar {
                flex-direction: column;
                height: auto;
                padding: 15px;
            }

            .navbar .nav-links {
                margin-top: 15px;
                gap: 12px;
                flex-wrap: wrap;
                justify-content: center;
            }
        }

        /* CONTENT */
        .container {
            max-width: 1100px;
            margin: 30px auto;
            background: var(--card);
            padding: 40px;
            border-radius: 24px;
            box-shadow: var(--shadow);
        }

        h1 {
            color: var(--wa-dark);
            text-align: center;
            font-size: 32px;
            margin-bottom: 10px;
        }

        .subtitle {
            text-align: center;
            color: #666;
            margin-bottom: 30px;
        }

        .summary-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
            gap: 20px;
            margin: 30px 0;
        }

        .summary-card {
            padding: 24px;
            border-radius: 16px;
            text-align: center;
        }

        .summary-card.recoleccion {
            background: var(--recoleccion);
            border: 2px solid #90caf9;
        }

        .summary-card.entrega {
            background: var(--entrega);
            border: 2px solid #a5d6a7;
        }

        .summary-card h3 {
            margin-bottom: 12px;
            color: var(--wa-dark);
        }

        .summary-card p {
            font-size: 36px;
            font-weight: bold;
        }

        .timeline {
            margin-top: 50px;
        }

        .timeline h2 {
            text-align: center;
            margin-bottom: 30px;
            color: var(--wa-dark);
        }

        .event {
            background: white;
            border-radius: 16px;
            padding: 20px;
            margin-bottom: 20px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.05);
            display: flex;
            gap: 20px;
            align-items: flex-start;
        }

        .event.recoleccion {
            border-left: 6px solid #2196f3;
        }

        .event.entrega {
            border-left: 6px solid #4caf50;
        }

        .event-icon {
            width: 50px;
            height: 50px;
            border-radius: 50%;
            background: var(--wa-green);
            color: white;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 24px;
            flex-shrink: 0;
        }

        .event.recoleccion .event-icon {
            background: #2196f3;
        }

        .event.entrega .event-icon {
            background: #4caf50;
        }

        .event-content {
            flex: 1;
        }

        .event-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 12px;
        }

        .event-type {
            font-weight: 600;
            font-size: 18px;
            color: var(--wa-dark);
        }

        .event-date {
            color: #666;
            font-size: 14px;
        }

        .event-details {
            margin-top: 10px;
        }

        .event-details ul {
            list-style: none;
            padding: 0;
        }

        .event-details li {
            padding: 6px 0;
            border-bottom: 1px dashed #eee;
        }

        .event-details li:last-child {
            border-bottom: none;
        }

        .event-details strong {
            color: var(--wa-dark);
        }

        .error {
            text-align: center;
            color: #d32f2f;
            background: #ffebee;
            padding: 20px;
            border-radius: 12px;
            margin: 20px 0;
        }

        .no-events {
            text-align: center;
            font-size: 20px;
            color: #4caf50;
            margin: 60px 0;
        }

        a.back {
            display: block;
            text-align: center;
            margin-top: 50px;
            color: var(--wa-dark);
            font-weight: 600;
            text-decoration: none;
            font-size: 17px;
        }
    </style>
</head>

<body>
    <!-- NAVBAR -->
    <nav class="navbar">
        <div class="logo-title">
            <div class="logo-small"></div>
            <span>Sistema de Inventario</span>
        </div>
        <div class="nav-links">
            <a href="/?client=<%= encodeURIComponent(client || '') %>">Volver al Conteo</a>
            <a href="/create_defect?client=<%= encodeURIComponent(client || '') %>">Reporte de Daño</a>
            <a href="/damage-breakdown?client=<%= encodeURIComponent(client || '') %>">Desglose de Daños</a>
            <a href="/log?client=<%= encodeURIComponent(client || '') %>" class="active">Bitácora de Recolección y
                Entrega</a>
            <a href="/dashboard?client=<%= encodeURIComponent(client || '') %>">Dashboard</a>
        </div>
    </nav>

    <div class="container">
        <h1>Bitácora de Recolección y Entrega</h1>
        <p class="subtitle">Cliente: <strong>
                <%= client %>
            </strong></p>

        <% if (error) { %>
            <div class="error">
                <%= error %>
            </div>
            <% } else if (allEvents.length===0) { %>
                <p class="no-events">
                    No hay registros de recolección o entrega para este cliente aún.
                </p>
                <% } else { %>

                    <!-- Summary Cards -->
                    <div class="summary-grid">
                        <div class="summary-card recoleccion">
                            <h3>Total Recolecciones</h3>
                            <p>
                                <%= totalRecolecciones %>
                            </p>
                        </div>
                        <div class="summary-card entrega">
                            <h3>Total Entregas</h3>
                            <p>
                                <%= totalEntregas %>
                            </p>
                        </div>
                        <div class="summary-card recoleccion">
                            <h3>Prendas Recogidas</h3>
                            <p>
                                <%= Number(totalPrendasRecogidas).toLocaleString('es-MX') %>
                            </p>
                        </div>
                        <div class="summary-card entrega">
                            <h3>Prendas Entregadas</h3>
                            <p>
                                <%= Number(totalPrendasEntregadas).toLocaleString('es-MX') %>
                            </p>
                        </div>
                    </div>

                    <!-- Timeline of Events -->
                    <div class="timeline">
                        <h2>Historial Completo</h2>
                        <% allEvents.forEach(event=> { %>
                            <div class="event <%= event.type %>">
                                <div class="event-icon">
                                    <%= event.type==='recoleccion' ? '↓' : '↑' %>
                                </div>
                                <div class="event-content">
                                    <div class="event-header">
                                        <div class="event-type">
                                            <%= event.typeLabel %>
                                        </div>
                                        <div class="event-date">
                                            <%= new Date(event.date).toLocaleDateString('es-MX', { year: 'numeric' ,
                                                month: 'long' , day: 'numeric' , hour: '2-digit' , minute: '2-digit' })
                                                %>
                                        </div>
                                    </div>
                                    <div class="event-details">
                                        <ul>
                                            <% Object.entries(event.articles || {}).forEach(([article, count])=> { %>
                                                <li><strong>
                                                        <%= article %>
                                                    </strong>: <%= count %> prenda<%= count> 1 ? 's' : '' %></li>
                                                <% }) %>
                                        </ul>
                                        <% if (event.EPCs && event.EPCs.length> 0) { %>
                                            <p style="margin-top: 12px; font-size: 13px; color: #777;">
                                                Total EPCs: <%= event.EPCs.length %>
                                            </p>
                                            <% } %>
                                    </div>
                                </div>
                            </div>
                            <% }) %>
                    </div>

                    <% } %>

                        <a href="/dashboard?client=<%= encodeURIComponent(client || '') %>" class="back">← Volver al
                            Dashboard</a>
    </div>
</body>

</html>